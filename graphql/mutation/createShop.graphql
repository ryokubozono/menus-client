mutation createShop(
  $uid: String!
  $name: String!
  $zip: String!
  $address: String!
  $tel: String!
  $note: String!
) {
  createShop(input: {
    uid: $uid
    name: $name
    zip: $zip
    address: $address
    tel: $tel
    note: $note
  }) {
    uid
    name
    zip
    address
    tel
    note
  }
}
